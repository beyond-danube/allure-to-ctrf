export interface AllureConvertionOptions {
  outputFolder: string
  outputFile: string
}

export const defaultOptions: AllureConvertionOptions = {
  outputFolder: 'ctrf',
  outputFile: 'ctrf-report.json'
}
